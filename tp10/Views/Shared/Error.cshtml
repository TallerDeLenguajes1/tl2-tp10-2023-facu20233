@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="text-center">
                <h2 class="text-danger">¡Ups! Algo salió mal.</h2>
                @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                {
                    <div class="alert alert-danger mt-4" role="alert">
                        @Model.ErrorMessage
                    </div>
                }
                <p class="mt-4">Lo sentimos, se ha producido un error inesperado.</p>
                @if (Model.ShowRequestId)
                {
                    <p class="mt-2">
                        <strong>Identificador de solicitud:</strong> <code>@Model.RequestId</code>
                    </p>
                }
                <p class="mt-4">Nuestro equipo ya ha sido notificado del problema. Por favor, inténtalo de nuevo más tarde o <a href="#">contáctanos</a> si el problema persiste.</p>
            </div>
        </div>
    </div>
</div>

@if (Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT") == "Development")
{
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <h3 class="text-center">Modo de desarrollo</h3>
                <p class="text-center">El entorno de desarrollo mostrará más información detallada sobre el error que ocurrió.</p>
                <p class="text-center">El entorno de desarrollo no debería estar habilitado para aplicaciones desplegadas. Para la depuración local, habilita el entorno de desarrollo configurando la variable de entorno <code>ASPNETCORE_ENVIRONMENT</code> a <strong>Development</strong> y reiniciando la aplicación.</p>
            </div>
        </div>
    </div>
}

